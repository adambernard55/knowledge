# Ignore all hidden files and folders at the root, except .github and .gitignore
.*
# Re-include .github and .gitignore so they are tracked
!.github/
!.gitignore

# Ignore all other files and folders at the root
/*

# Explicitly track the knowledge base folder and its contents
!/kb/

# Explicitly ignore Obsidian's folder, trash, and any temporary files
# This is the most important part for plugin compatibility
.obsidian/
.trash/
*.tmp
*.mdb